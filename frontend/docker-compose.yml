services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_GRAPHQL_ENDPOINT: ${VITE_GRAPHQL_ENDPOINT}
    ports:
      - '3001:8080'
    restart: unless-stopped
    env_file:
      - .env.pre
