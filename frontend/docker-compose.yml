services:
  frontend:
    build:
      context: .
      target: development
    ports:
      - "3001:3000"
    environment:
      - VITE_API_BASE_URL=http://localhost:8002/api/v1
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
